# Build-time dependencies for the local pipeline + snapshot generator
# (parsing PDF/DOCX/XLSX attachments). The Vercel serverless functions
# in api/ serve a prebuilt snapshot and are stdlib-only.
pypdf>=4.2.0
python-docx>=1.1.0
openpyxl>=3.1.0